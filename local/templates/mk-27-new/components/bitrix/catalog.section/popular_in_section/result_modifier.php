<?if(!defined("B_PROLOG_INCLUDED") || B_PROLOG_INCLUDED!==true)die();
/*
global $USER;
if (!$USER->IsAdmin())
{
    foreach ($arResult["ITEMS"] as $key => $val)
    {
        if (!$val["PRICES"]["1C_PRICE"]["VALUE"])
        {
            unset ($arResult["ITEMS"][$key]);
        }
        if (!$val["DETAIL_PICTURE"]["SRC"])
        {
            unset ($arResult["ITEMS"][$key]);
        }
        if ($val["PRODUCT"]["QUANTITY"] < 1)
        {
            unset ($arResult["ITEMS"][$key]);
        }
    }
}
*/
?>